# ============================================================================
# SIFT Image Stitching Tool - Production Dependencies
# ============================================================================
#
# このファイルには、アプリケーション実行に必要な最小限の依存関係が含まれています。
# 開発用のツール（PyInstaller等）は requirements-dev.txt を参照してください。
#
# インストール方法:
#   pip install -r requirements.txt
#
# 動作確認済み環境:
#   - Python 3.11+

#   - Windows 10/11, macOS, Linux
#
# ============================================================================

# ----------------------------------------------------------------------------
# Core Image Processing
# ----------------------------------------------------------------------------

# OpenCV with SIFT support (contrib モジュールが必要)
# SIFT特徴量検出とマッチングに使用
opencv-contrib-python==4.12.0.88

# NumPy - 数値計算とArray操作
# OpenCVの依存関係でもある
numpy==2.2.6

# SciPy - 科学計算ライブラリ
# scikit-imageの依存関係
scipy==1.16.3

# scikit-image - 画像処理ライブラリ
# 画像品質評価（SSIM等）に使用
scikit-image==0.25.2

# ----------------------------------------------------------------------------
# Web Framework
# ----------------------------------------------------------------------------

# Flask - 軽量Webフレームワーク
# APIサーバーとして使用
Flask==3.1.2

# Flask-CORS - Cross-Origin Resource Sharing対応
# フロントエンドとの通信に必要
flask-cors==6.0.1

# ----------------------------------------------------------------------------
# Flask Dependencies (自動的にインストールされますが、明示的に記載)
# ----------------------------------------------------------------------------

# Werkzeug - WSGIユーティリティライブラリ（Flaskの依存関係）
Werkzeug>=3.1.3

# Jinja2 - テンプレートエンジン（Flaskの依存関係）
Jinja2>=3.1.4

# Click - CLIツール作成ライブラリ（Flaskの依存関係）
click>=8.1.7

# ItsDangerous - データ署名ライブラリ（Flaskの依存関係）
itsdangerous>=2.2.0

# MarkupSafe - 文字列エスケープライブラリ（Jinja2の依存関係）
MarkupSafe>=3.0.2

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
#
# バージョン指定について:
# - == : 厳密なバージョン指定（再現性を保証）
# - >= : 最小バージョン指定（互換性のある新しいバージョンを許可）
#
# opencv-contrib-python について:
# - opencv-python では SIFT が使用できないため、contrib版が必須
# - ライセンス: OpenCV は Apache 2.0、SIFT は特許問題解決済み（2020年以降）
#
# scikit-image について:
# - 主に tools/analyze_stitched.py で使用
# - 本体アプリケーションでは必須ではないが、品質評価に有用
#
# ============================================================================
